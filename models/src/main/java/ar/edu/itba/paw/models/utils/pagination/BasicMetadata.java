package ar.edu.itba.paw.models.utils.pagination;

public class BasicMetadata extends Metadata{
    public BasicMetadata(int currentPage, int totalResults, int pageSize) {
        super(currentPage, totalResults, pageSize);
    }
}